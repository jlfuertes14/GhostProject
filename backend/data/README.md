# Data Directory

Place your `dpwh_flood_control_projects.csv` file here.

This directory is the single source of truth for the raw dataset used by:
- The Google Colab ML notebooks
- The FastAPI backend (converted to DuckDB/SQLite on startup)
